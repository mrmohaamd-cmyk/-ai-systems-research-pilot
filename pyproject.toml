[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "critique-revision-pilot"
version = "0.1.0"
requires-python = ">=3.10"
description = "Bounded evaluation harness for critique-and-revision workflows"

[tool.setuptools]
packages = ["pilot"]
